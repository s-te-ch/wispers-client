// -*- protobuf -*-
// On-device persistent storage format for node registration.
// Field numbers must remain stable across versions.

syntax = "proto3";
package connect.storage;

message NodeRegistration {
    string connectivity_group_id = 1;
    int32 node_number = 2;
    string auth_token = 3;
    string attestation_jwt = 4;
}
